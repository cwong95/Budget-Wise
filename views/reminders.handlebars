<main class='reminders-page'>
  <section class='header-section'>
    <h1 class='page-title'>Reminders</h1>
    <p class='description'>Manage your reminders — acknowledge or act on them.</p>
  </section>

  {{#if error}}
    <section class='error-section'>
      <p class='error-message'>🛑{{error}}</p>
    </section>
  {{/if}}

  <hr />

  <h2>🔔 Due Reminders</h2>
  {{#if reminders.length}}
    <section class='inapp-reminders'>
      <ul class='reminders-list'>
        {{#each reminders}}
          <li class='reminder-item'>
            <div class='reminder-main'>
              {{#if (eq this.type 'on')}}📅 Due today{{else}}⏳ Upcoming{{/if}}
              —
              <strong>{{this.utilityName}}</strong>
              bill of
              {{formatMoney this.amount}}
              (<span class='reminder-date'>{{formatDate this.reminderDate}}</span>)
            </div>
            <div class='reminder-actions'>
              <form action='/reminders/{{this._id}}/ack' method='POST' style='display:inline;'>
                <button type='submit' class='btn btn-secondary'>Acknowledge</button>
              </form>
              <a href='/utilities/{{this.utilityId}}/bills' class='btn btn-link'>Open bills</a>
            </div>
          </li>
        {{/each}}
      </ul>
    </section>
  {{else}}
    <p>No reminders due right now.</p>
  {{/if}}

</main>